## Very, very spermous template for compeptitive programming.
### Features
#### Input parsing
- `cp::StandardInputParser` supports all input types and is 2x faster than `cin` in C++
- `cp::FastInputParser` supports only numeric types and its 10% faster than handmade input parsing in C++ (idk how bru)
#### Testing and testgen
Soon twinkies trust me 🔜
